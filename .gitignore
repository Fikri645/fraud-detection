# Data — too large to commit (regenerate via scripts/download_data.py)
data/raw/*.csv
data/processed/*.parquet
data/processed/*.csv

models/*.npy

# Committed model artifacts (all small, used by the app / for reproducibility):
#   - lgbm_fraud.joblib (4.4 MB) powers live scoring
#   - autoencoder.pt (33 KB), gnn_fraud.pt (82 KB)
#   - *_meta.json / *_report.json / *_benchmark.json feed the dashboard tabs

# MLflow
mlruns/
mlartifacts/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
.coverage
coverage.xml
htmlcov/

# Envs
.venv/
venv/
env/

# OS / IDE
.DS_Store
.vscode/
.idea/
*.swp
